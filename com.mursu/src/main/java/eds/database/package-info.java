/**
 * Persistence and reporting infrastructure used by the MVC model layer.
 *
 * <p>This package stores simulation runs, trades, and aggregated metrics in SQLite
 * and exposes query helpers for loading historical results back into the
 * application.</p>
 */
package eds.database;
